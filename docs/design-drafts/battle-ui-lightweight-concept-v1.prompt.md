# Battle UI lightweight concept v1

Generated with the built-in ImageGen workflow from
`tmp/imagegen/battle-ui-current-reference.png`.

## Prompt

```text
Use case: ui-mockup
Asset type: static 1280×720 battle HUD concept for the Little Math Adventure game
Input image 1: EDIT TARGET. Preserve the battle background, ground, sky, trees, hero, pet, enemy, their exact positions, scale, colors, and the bright friendly rendering. Replace only the current UI overlay.

Primary request: redesign the battle UI so it feels much lighter, airier, and easier for a child to scan. Match the supplied game UI reference and preserve one shared visual system. Use one canonical thin medieval frame language across related controls: dark leather, narrow warm bronze/wood edging, tiny blue crystal accents, restrained highlights. Every circular socket must remain a mathematically true circle, never an ellipse.

Composition:
- Keep at least 65% of the battle scene visually unobstructed.
- Replace the large player and enemy status plaques with two slim floating health ribbons placed near their respective characters. Each ribbon has one compact true-circular medallion (heart/hero on the left, enemy portrait on the right) and a very thin colored health line; minimal metal, almost no parchment.
- Replace the three separate long bottom buttons with ONE shallow shared action dock centered at the bottom, maximum about 560 px wide and 100 px tall. The dock is a low-profile dark leather strip with a thin bronze rim, not a large plaque.
- Put three true-circular action sockets into this shared dock: a larger central sword action, a smaller red potion action on the left with a tiny counter badge, and a smaller active-pet portrait or paw action on the right with a tiny damage badge. Keep generous spacing and clear silhouettes.
- Show blocking only as a compact cool-blue shield crest, maximum about 230×58 px, floating just above the shared dock or at top center. It should feel temporary and lightweight, not like another large panel.
- Keep a small unobtrusive back control in the top-left.

Mandatory production-system constraints: create only structural frames and icon placement; do not bake any words, letters, numbers, labels, pseudo-text, runes, or watermark into the image. Leave small clean blank areas or tiny blank badges where runtime text and numbers would later be rendered in code. Related controls must reuse the same canonical rim/source. Stateful icons would later use aligned normal/active pairs with identical silhouette, pose, scale, cell size, and placement; only light or glow may change. Hover geometry must remain stable. Text, hit areas, layout, disabled state, localization, and animation belong in code.

Style: polished 2D children’s medieval-fantasy game UI, friendly and tactile, thin bronze, dark leather, subtle wood, small blue crystal details, crisp readable icons, soft shadows only.

Avoid: large parchment plates, thick heavy frames, three separate long action buttons, oversized metal, bulky top-center banner, repeated rectangular boxes, stretched circular frames, ellipses, clutter, text of any kind, fake lettering, decorative runes, watermark, changes to the characters or environment.
```
