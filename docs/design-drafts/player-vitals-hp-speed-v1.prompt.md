# Player vitals HP + speed panel v1 — ImageGen prompt

Mode: built-in ImageGen, isolated component derived from `battle-ui-lightweight-concept-v2.png`.

Create one isolated production-ready fantasy game UI component, using the attached battle mockup only as the exact style and proportions reference. Asset type: a combined PLAYER HEALTH + SPEED BONUS HUD panel for a child-friendly medieval adventure game.

Mandatory production constraints: follow one canonical visual language across all parts; build the control as stable reusable layers; generated text must not appear; runtime labels, numbers, hit areas, localization, disabled states and animations will be supplied in code; hover geometry must remain stable; do not imply root scaling or whole-bitmap swapping. Keep the component suitable for later layer extraction and deterministic alignment.

Composition: exactly ONE compact horizontal component, centered with generous empty padding. On the left, one TRUE CIRCULAR bronze-and-dark-leather medallion with a simple red heart icon. From the medallion extend two slim horizontal tracks to the right. The upper track is the primary health bar: dark leather and bronze frame, with a partially filled bright green inner bar. Directly beneath it is a much thinner bonus-speed charge track with EXACTLY THREE clearly separated small diamond/rune segments: first two glowing warm gold, third dark and uncharged. The lower speed track must visibly belong to the same frame while remaining a distinct secondary meter. Use the same bronze rims, dark stitched leather, tiny blue diamond pins and polished cartoon rendering as the attached mockup. Maintain a lightweight silhouette: thin rails, no bulky plaque, no portrait, no nameplate, no oversized decorations. Perfectly circular medallion, not an ellipse. Straight horizontal alignment and clean spacing.

Background and extraction requirements: render on a perfectly flat, uniform pure chroma green `#00FF00` background across the entire canvas. No gradient, texture, horizon, floor, cast shadow, reflection, ambient color variation, green spill, or scenery. Do not let any part of the component touch the canvas edges.

Strict exclusions: no words, no letters, no numbers, no pseudo-text, no watermark, no enemy portrait, no character portrait, no extra buttons, no additional loose icons, no scenery. Only the red heart and the three speed-charge runes may be symbolic icons. Output a clean front-facing orthographic UI asset concept with crisp edges and consistent polished 2D cartoon game art.
