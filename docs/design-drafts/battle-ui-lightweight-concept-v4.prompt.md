# Battle UI lightweight concept v4 — ImageGen prompt

Mode: built-in ImageGen precise-object edit.

References:

- `battle-ui-lightweight-concept-v3.png` — edit target.
- `prep-sword-normal-v2.png` — exact ShopScene sword icon.

Change only the sword inside the bottom-left preparation socket. Replace it with
the exact ShopScene sword design while preserving the existing round frame and
three preparation-charge diamonds. Keep the selected-target sword above the
enemy, the large central attack sword, four speed slots, all other HUD elements,
characters, background, lighting and composition unchanged.

The corresponding future shield state must use the existing
`prep-shield-normal-v2` / `prep-shield-active-v2` pair inside the same frame.
No text, numbers, watermark, new glow, new controls or additional decoration.
