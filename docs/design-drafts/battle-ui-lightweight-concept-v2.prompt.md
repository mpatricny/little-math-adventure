# Battle UI lightweight concept v2 — ImageGen prompt

Mode: built-in ImageGen, edit of `battle-ui-lightweight-concept-v1.png`.

Create a refined static battle UI mockup based on the supplied lightweight battle concept. Preserve the bright forest battlefield, character placement, slim health ribbons, shared bottom action dock, central shield indicator, and round back button.

Use one canonical medieval UI language throughout: dark stitched leather, thin bronze edges, small blue diamond pins, true circular sockets, and light polished cartoon rendering. Keep every control compact and avoid heavy panels. Generated text, labels, numbers, pseudo-text, and watermarks are forbidden; those belong to runtime UI.

Refinements:

- Add a small round pause button at the top right.
- Player HUD: retain the heart medallion and green health bar; add a distinct thin speed-bonus charge track directly below it with exactly three separated runes/segments. Show two charged gold and one dark.
- Enemy HUD: remove the monster portrait completely. Use only a small red crystal/damage emblem at the left of a slim red health bar.
- Battle preparation: add a compact sword-preparation indicator at the lower left, separate from the main action dock. It contains one sword socket and exactly three small charge runes; two glow amber and one is dark. It must also work later with a shield icon.
- Shared bottom dock: potion on the left, large primary sword action in the center, active pet on the right. Give the main sword a warm gold ready glow to demonstrate a fully charged fast-answer bonus.
- Pet socket: show the actual active green slime portrait, with a subtle green pet-turn glow and a small blank circular badge reserved for runtime damage. With no pet, this becomes a dark paw silhouette.
- Potion socket also has a small blank circular badge reserved for runtime quantity/state.
- Retain the compact shield crest at the top center.

All circular elements must remain true circles, never ellipses. Keep stable hover geometry and reserve animation, hit areas, text, values, disabled state, and localization for code.
